# AGENTS.md

本 repo 的 agent 指南以 **`CLAUDE.md`** 為單一真實來源。所有 agent 工具（Claude Code、Cursor、Codex 等）一律以該檔為準；本檔只是入口別名，**不重複內容以免與 `CLAUDE.md` 漂移**。

- 專案結構、共用技術棧、建置慣例、架構要點、常用指令、慣例強制與漂移掃描 → `CLAUDE.md`
- 各模組專屬規則（環境變數、進入點、陷阱、原始碼佈局） → `.claude/rules/<模組>.md`
